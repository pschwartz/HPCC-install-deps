This is a script to install all build reqs for the HPCC-Platform.

Run it with the following:

INSTALL:
sudo ./install-deps [centos|ubuntu] <version>


version files and additional source provided currently for the 
following distros:

centos 5.x, 6.x
ubuntu 10.04, 12.04, 12.10, 13.04


